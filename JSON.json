// Assigning json for server

def write_json(new_data, filename='JSON.json'):
with open(filename,'r+') as file:
file_data = json.load(file)
content['Entries'].append(new_data)
file_data.update(content)
file.seek(0)
json.dump(file_data, file, indent = 4)

f = open('JSON.py', 'r+')
content = json.load(f)

out_file = open('JASON.json', 'w')


for c in content:

for e in content['Entries']:

    # if email == e['Email']:
    #     print('Please enter your password.')
    #     password = input('Enter password: ')
    #     if password == e['Password']:
    #         print('Welcome,', name, 'you are now logged in!')
    #     else:
    #         print('Wrong password.')
    #     break

    # else:
    #     print('Your name is not in the file.')
    #     print('You need to register.')
        new_name = nam
        new_email = email
        new_DOB = DOB
        new_contact = contact
        new_password = password
        id = len(content['Entries'])
    
        new_full = {"User name":f"{new_name}",
                        "Email":f"{new_email}",
                        "DOB":f"{new_DOB}",
                        "Contact":f"{new_contact}",
                        "Password": f"{new_password}",
                        "ID": f"{id + 1}"
                        }

write_json(new_full)
print(new_full)
print('Your name is added in the file.')

